# gotchas-plugins.md — moved verbatim from AGENTS.md (2026-09-23)

> These sections were moved OUT of the root `AGENTS.md` to keep the
> system-prompt injection under its 100k-character budget. Content is
> **verbatim** — nothing was dropped. `AGENTS.md` keeps a one-line index
> of every ⛔/⚠️ rule pointing here.

### Plugin System
- **Hook-triggered script execution uses the WorkDir SNAPSHOT, never the shared pctx (cross-session race fix).** `PluginContext.workingDir` is a SHARED mutable field that `RefreshWorkDir` overwrites on every session's Cd — a hook fired by session A could execute in session B's directory (two "official self-confession" comments in script_runtime.go documented the symptoms: OnWorkDirChanged's pending-set and renderByWidgetAndWorkDir's "WITHOUT modifying the shared PluginContext"). The fix: `HookPayload.WorkDir` carries the TRIGGER-TIME cwd snapshot (hooks `BasePayload.CWD` — engine fills it per event; `plugin_bridge` extracts `"cwd"` into `payload.WorkDir`), the hook handler pins it into `pendingDirs` (`addPendingDir` — the async refresh loop consumes the pinned dir even if pctx was overwritten before consumption), and the sync-hint path prefers `hp.WorkDir` over `pctx.WorkingDir()`. The pctx fallback stays for widget/interval triggers (no hook payload). Tests: `plugin/script_runtime_wd_race_test.go` (snapshot beats pctx overwrite, missing-snapshot falls back, pin isolation).
- **核心 RPC 方法名绝不能含点号（`.`）——否则被 `web_plugin_rpc` 误路由到插件进程。** 前端 `FetchRpcTransport`（`web/src/plugin-runtime/rpc.ts`）用「method 是否含 `.`」判断路由：含点 → 包装进 `web_plugin_rpc`，后端 `resolvePluginRPCMethod` 按最长前缀匹配插件 ID 路由到插件。因此**核心 RPC（前后端固定 handler，不走插件进程）必须用无点名字**——插件配置的核心 RPC 是 `plugin_config` / `plugin_config_set`，不是 `plugin.get_config`（后者被误当插件 ID=`plugin`，`ctx.config.get()` 静默失败、被 `.catch` 吞掉，配置永远用默认值——本次 iteration-stats「关 TTFT 无效」的真凶）。
- **`plugin/permissions.go` 的 `allPermissions` 白名单必须与前端 `Permission` 类型逐字同步。** 新增前端权限（如 `'config'`）时后端 `allPermissions` 漏注册 → `validateManifest` 拒收该插件（`unknown permission "config"`，reload 失败）。白名单编译进 Go 二进制，修复后**必须重启 server** 才生效。前端 7 个权限 `events/commands/rpc/plugins/ui/config` + 后端各 `PermXxx` 常量都要一一对应；守护测试 `plugin/permissions_test.go:TestAllFrontendPermissionsRegistered`。
- **⛔ 插件 i18n 是【平台通用能力】：文案随插件清单走，绝不允许写进宿主的 `web/src/i18n/*.ts`（2026-09-19 用户要求）。** 契约四处：① **声明** —— `plugin.json` 的 `web.i18n: { "<locale>": { "<key>": "<text>" } }`；后端 `WebPluginDecl.I18n json.RawMessage` **只做原样透传**（与 `contributes` 同一「单一门控」：后端不解析内容），`serverapp/rpc_table.go` 的 `web_plugin_list` 把它带给前端（前端 `PluginManifest.i18n` 同步）。② **解析** —— `web/src/plugin-runtime/i18n.ts` 的 `createPluginI18n(table, readLocale)`：回退链 **当前 locale（精确 → 语言前缀：`zh-TW`/`zh-HK` 命中 `zh-CN`，`zh` 短前缀也能被 `zh-CN` 命中）→ `en` → 表里首个可用 locale → `fallback` 参数 → key**；**永不抛错、永不返回 undefined**；`locale` 是**读取时快照**（宿主切语言即时生效）。③ **注入** —— `PluginContext.i18n: I18nAPI`，**所有插件可用、不设权限**（纯呈现能力），`buildContext` 无条件注入；装配点 `plugin-runtime/index.ts`：`createPluginI18n(effective.i18n, () => i18n.language)`。④ **插件侧用法** —— `activate(ctx)` 拿到 `ctx.i18n`，视图经模块级单例调用（ssh-runner 的 `shared.ts::t(key, fallback, params?)` 即此模式，**已摘掉宿主的 `window.__xbot_i18n__` 桥**）。**迁移实例**：ssh-runner 的 94 key × zh-CN/en/ja 全部搬进 `plugins/xbot-ssh-runner/plugin.json` 的 `web.i18n`，宿主三语里的 `sshRunner` 块**整体删除**（宿主里 `grep -c sshRunner` 必须为 0）。守护：`web/src/plugin-runtime/i18n.test.ts`（12 例：精确/语言前缀/回落 en/回落首个可用/fallback/key 兜底/空表不抛错/locale 实时快照/下划线归一化）。**两条载体（2026-09-19 补齐，别混淆）**：① **经 URL 加载的独立插件**（genui / git-fancy / ssh-runner / iteration-stats / session-stats 等，产物在 `/plugins/<id>/web/*.js`）→ 文案在**插件清单** `web.i18n`，源码经 `ctx.i18n` 解析（genui 的模块级 `i18nRef`、git-fancy 的 `setI18n` 都是此模式；**宿主 `window.__xbot_i18n__` 桥是历史遗留，禁止新增使用**）；② **宿主静态 import 的内置视图/内置插件清单**（plugin-manager / skill-manager / git-info / ambience，随主 bundle 打包 —— 既拿不到 `ctx` 也拿不到插件清单）→ 文案在**宿主 i18n**（`import i18n from '@/i18n'`，或组件内 `useI18n()`），且必须 `web/src/i18n/{zh-CN,en,ja}.ts` 三语同步。守护：`plugins/xbot-{genui,git-fancy,ssh-runner}/manifest_i18n_test.go`（三语 key 集合必须互为子集 + 条数下限；表缺失/漏译此前是完全静默的）。
- **⛔ 语言切换后文案必须**动态**更新（2026-09-19 用户实测「改了语言插件没动态变化」；根因：文案解析虽是「调用时读语言」，但**没有订阅就没有重渲染/重算** ⇒ 画面定格旧语言）。** 五条契约，缺一条就再现：① **宿主唯一 seam** = `web/src/i18n/index.ts` 的 `onLocaleChanged(cb)`（内部即 i18next `languageChanged`；**禁止各处自己 `i18n.on`**）。② **登记表重算** = `plugin-runtime/viewRegistrySync.ts` 的 `usePluginViewRegistrySync(runtime)`（在 `PluginRuntimeBootstrap` 挂载）：view 集合变化 **+ 语言变化**都重跑 `createViewRegistrySync().sync()`，按当前语言重新 `resolvePluginText` 后覆盖登记 `panelRegistry`/`layoutRegistry`（`registerPanel`/`register` 幂等覆盖 ⇒ **只换文本，不重置用户布局覆盖/顺序**）；`usePluginViewPanels.ts` 同样订阅（手机工具 tab / rail 标题）。③ **插件视图重挂载** = `PluginView.tsx` 用 `useLocale()`（`plugin-runtime/useLocale.ts`）作 React `key`：URL 加载的插件是独立 bundle、`ctx.i18n.t()` 在渲染时取值，**宿主不重新渲染它 ⇒ 永远旧语言**；remount 是唯一对**已发布、不改代码**的插件也生效的机制（代价：切语言重置该视图局部状态，语言切换是罕见显式操作）；`ViewSlot.tsx` 同契约。④ **插件事件（可选细粒度）** = `EventMap['i18n.localeChanged']`（`plugin-api/events.ts`）由 Bootstrap 经 `usePluginLocaleEvent(runtime.events)` 广播 `{ locale }`；插件可 `ctx.events.on('i18n.localeChanged', …)`（需 `events` 权限）。⑤ **内置清单必须【读取时】求值** = `src/plugins/{manager/pluginManager,xbot-skill-manager/skillManager,session-stats/sessionStats}.ts` 的 `name`/`description`/view `title` 一律写成 **getter**（`get name() { return i18n.t(…) }`）—— 模块顶层 `i18n.t()` 在 import 时定格成加载时的语言，**即使登记表重算也读不到新文本**；`ContributionRegistry.listStates()` 的 `name` 同样读取时取 `r.manifest.name`（不落注册快照）。守护：`web/src/plugin-runtime/viewRegistrySync.test.ts`（切语言 ⇒ panelRegistry+layoutRegistry 标题双更新 / 幂等且保留用户布局 / dispose 清理）、`localeEvent.test.ts`（`{locale}` 广播 + 卸载退订）、`bootstrapLocaleWiring.test.tsx`（**接线守卫**：启动器真的挂了广播与登记表同步 —— hook 层全绿但忘调用 = 功能不存在）、`PluginView.test.tsx`（文案随语言变化 + key 驱动 remount）、`usePluginViewPanels.test.tsx`（已渲染的 hook 结果随语言更新）、`src/plugins/manager/builtinManifestI18n.test.ts`（三个内置清单 + `listStates()` 读取时解析）。四组关键用例均已**变异自证**（撤掉订阅 / key / getter / 接线 ⇒ 对应用例必红）。
- **Plugin system is opt-in** — only activates when `plugins.enabled: true` in config.json. No plugin loading happens without explicit user consent.
- **`pm.workDir` is `atomic.Value` (not `string`).** `activate()` may be called while `pm.mu` write lock is held — reading workDir must be lock-free. Never change it back to `string` or `activate`/`InstallPlugin` will deadlock.
- **`runAndUpdate()` does NOT write global slot cache.** It calls `NotifyUpdated()` instead of `UpdateWidget()`. Writing global cache causes cross-session overwrites (session B's git branch overwrites session A's).
- **CLI WS clients must NOT auto-subscribe to senderID ("admin").** `client_type=cli` connections skip p2p subscribe. Subscribing CLI to "admin" causes `PushPluginWidgetsPerSession` to send stale content to wrong windows.
- **`PushPluginWidgetsPerSession` skips non-path chatIDs.** Only chatIDs starting with `/` are session chatIDs. Virtual chatIDs like "admin" or "web-123" are not rendered.
- **`OnPluginWidgets` callback filters by chatID.** Client-side rejects pushes for other sessions. Double protection against cross-session widget corruption.
- **Script plugin outputs map is per-workDir.** `RenderForWorkDir(width, workDir)` reads `outputs[workDir]`. `Render()` falls back to shared `pctx.WorkingDir()` — never use for remote multi-session.
- **`HookPayload.ToolOutput` is truncated to 8KB.** Don't rely on it for full file content. Plugins needing full output should use dedicated tool result channels.
- **PluginManager.ActivateAll() collects capabilities; WireAll() connects them.** Never call registerCapabilities manually — WireAll is the single integration point.
- **PluginEntry.stateMu protects state transitions.** Use CAS pattern (check state → set activating → set active/error) to prevent concurrent activation races.
- **gRPC plugin processes are killed on timeout/cancellation.** The `call()` method kills the process and marks it as not-running to prevent goroutine leaks from blocked stdout reads.
- **PluginToolBridge auto-detects PluginToolV2.** If a plugin tool implements V2, the bridge passes ToolCallContext. Otherwise falls back to V1 Execute(ctx, input).
- **Plugin IDs validated with regex `^[a-zA-Z0-9][a-zA-Z0-9._-]{0,127}$`.** This prevents path traversal, null bytes, and injection attacks in storage paths.
- **Storage files use 0600 permissions and atomic write (tmp+rename).** Never use 0644 for plugin storage.
- **WASM runtime is skeleton-only.** It compiles and loads but Activate() is a no-op. Phase 2 requires wazero dependency.
- **PluginContext provides 4 type-safe Storage helpers:** `StorageInt`, `StorageBool`, `StorageJSON`, `StorageGetJSON`. These wrap the base `StorageAccessor` with parse/unmarshal and return typed results. Failed parses return zero-value + false (not errors) for Int/Bool, and errors for GetJSON.
- **Auto-retry runs in a background goroutine.** `SetAutoRetry(true, maxRetries)` starts `retryLoop` with exponential backoff (1s→30s cap). **`DeactivateAll()` cancels the retry context** — if you call `activate()` manually after `DeactivateAll()`, you must re-enable auto-retry or failed plugins won't recover automatically.
- **Manifest `timeout` field accepts Go duration strings** (`"30s"`, `"1m"`, `"500ms"`), parsed via `time.ParseDuration`. Empty or missing defaults to `DefaultPluginTimeout` (30s). Max allowed: 5 minutes.
- **EventBus requires `bus.plugin` permission** in addition to `bus.read`/`bus.write`. Subscribe needs `bus.plugin` + `bus.read`; Publish needs `bus.plugin` + `bus.write`. This separates plugin-to-plugin events from the core message bus.
- **`InstallPlugin` uses `filepath.EvalSymlinks`** to resolve the real directory path before deletion check, preventing symlink-based path traversal attacks. Only directories under `xbotHome` are deleted.
- **`WatchConfig` polls config.json every 30 seconds** (configurable, min 5s). It compares `plugins.disabled_plugins` lists via diff and reactively deactivates newly disabled / activates newly enabled plugins. Returns a stop channel for shutdown.
- **DependencyResolver uses Kahn's algorithm (BFS topological sort).** Circular dependencies return an error (not panic). `AddManifest` with duplicate ID replaces the existing entry. Resolve() returns activation order — plugins with no dependencies first, then in dependency order.
- **Profiler is safe for concurrent use** (sync.Mutex). `Profile(pluginID)` returns a **copy** of PluginProfile — safe to mutate without affecting internal state. Unprofiled plugins return zero-value PluginProfile.
- **ExportConfig acquires RLock on PluginManager.** Must not be called while holding a write lock (e.g., inside custom Activate/Deactivate that calls pm.mu.Lock). ImportConfig acquires write lock internally — do not nest inside another write-locked operation.
- **MockPlugin/MockTool chain API returns the same pointer** — each `With*` call mutates and returns `*MockPlugin`/`*MockTool`. Do not share a single mock across parallel tests without cloning.
- **PluginRegistry MVP only supports local sources** for installation. Search operates on locally installed plugins only. GitHub/URL sources are defined but InstallFromSource is not yet implemented — Phase 3 scope.
- **Plugin migration：`RegisterMigration` + `RunMigrations`（`plugin/migration.go`，无 `Migrator` 结构体）。** 迁移累积在按 pluginID 索引的全局注册表（mutex 保护）中；`RunMigrations(ctx, pluginID, currentVersion, storage)` 按 semver 排序（先 FromVersion 后 ToVersion），只执行链上待处理的迁移（`FromVersion` 必须等于最后应用的 `ToVersion`；`ToVersion <= currentVersion`；已应用/未来版本跳过；`FromVersion > ToVersion` 报错）。已应用 ID 记录在插件存储的保留键 `_migrations`（`migrationRecord{Applied []string}`）。每个步骤事务式执行：内存备份（`backupStorage`，排除 `_migrations`）→ `Migrate` → 保存记录；任一步失败即 `restoreStorage` 恢复备份（回滚）。**不存在磁盘 backups 目录**——旧版描述 `~/.xbot/plugins/<id>/backups/<version>/` 与实际代码不符。生产代码尚无调用点：插件在 `Activate` 中自行调用 `RunMigrations`。
- **`pluginLogger` writes ONLY to per-plugin log file — NOT to global logrus.** Plugin operational logs (tool/hook registration, script execution, widget refresh) are isolated in `~/.xbot/plugins/<id>/logs/`. This keeps the main xbot log clean. Only framework-level lifecycle events (discovered/activated/deactivated/failed) use global `log.Xxx()` in `manager.go`. Script runtime logs go through `p.logger()` (helper with noopLogger fallback for tests). If `fileOut == nil` (writer creation failed), `pluginLogger.emit()` falls back to global logrus so no logs are lost.
- **`toolHint` zone plugins run synchronously on PostToolUse hook.** When `isHintPlugin=true`, the hook trigger runs `runScript` inline (not via triggerCh). The engine calls `PluginManager.GetToolHints()` immediately after the hook returns to populate `ToolProgress.ToolHints`. **`GetToolHints()` consumes (clears) the hint after reading** to prevent stale content from attaching to the next tool.
- **`snapshotIterationChange` must include ActiveTools(done).** When an iteration ends, completed tools may still be in `ActiveTools` (status=done) rather than `CompletedTools` (which is populated later by `progressFinalizer`). Only checking `CompletedTools` loses ToolHints data.
- **Do NOT use glamour to render diff inside progress panel.** Glamour's output (background fills, margins, line wrapping) corrupts the progress panel border layout. Use direct ANSI coloring (`renderDiffANSI`) with width truncation instead.
- **`runScript` must `os.Stat(workDir)` before setting `cmd.Dir`.** On Windows parallel tests, temp dirs may be cleaned up before the script runs, causing `chdir` failure. If dir doesn't exist, skip setting `cmd.Dir` and run in plugin's own directory.
- **`runScript` injects `XBOT_WIDGET_ID` env var.** When a plugin declares multiple widgets, each widget gets a `widgetAdapter` that passes its `widgetID` to `runScript`. Scripts can branch on `$XBOT_WIDGET_ID` to produce different output per widget. Without this, all widgets of the same plugin produce identical output (the reported "5 duplicates of `main +10 ↑13`" bug). The `outputs` cache is a two-level map: `workDir → widgetID → output`.
- **Platform-specific entry points.** `PluginManifest` supports `entry_windows`, `entry_darwin`, `entry_linux` fields that override the generic `entry` for matching OS. `scriptPlugin.resolvedEntry()` selects the correct entry at runtime. Use this when a plugin needs bash on Unix but PowerShell on Windows. Generic `entry` is always the fallback.
- **Session context env vars for plugins.** `runScript` injects `XBOT_MODEL`, `XBOT_MAX_CONTEXT`, `XBOT_TOKEN_USAGE` (format: `"prompt/completion"`), `XBOT_PROMPT_TOKENS`, `XBOT_COMP_TOKENS`. Data flows via `hooks.SessionContext` (mutable pointer in context, updated by engine after each LLM call and compression) → `plugin_bridge.go` extracts into `HookPayload.Extra` → `runScript` serializes to env vars. Widgets can display model name, context usage bar, and token costs.
- **Web 插件 v2（类型即契约，前端 ESM 插件运行时）。** 设计文档 `docs/agent/web-plugin-system.md`。核心：前端插件 = 编译后的 ESM 模块 + 类型化 manifest（`@xbot/plugin-api`），直接在前端加载执行；**后端无 JS 虚拟机**（后端插件仍是 Go/stdio 进程模型，只做清单下发 + 事件桥 + RPC 路由 + 静态托管）。关键文件：类型包 `web/src/plugin-api/`（manifest/context/events/rpc/renderer/components + `types.test.ts` 13 个 `@ts-expect-error` 编译期断言）；运行时 `web/src/plugin-runtime/`（registry/events/commands/rpc/loader/context/state/ui/plugins/sanitize/ViewSlot/index/usePluginRuntimeHost）；内置插件管理面板 `web/src/plugins/manager/`（自举）；git-info fancy 视图 `web/src/plugins/git-info/GitStatusPanel.tsx`；内置技能管理面板 `web/src/plugins/xbot-skill-manager/`（skill 核心 RPC 无点号直传 /api/rpc + **SKILL.md 面板内渲染**（`skill_get_content` + `MarkdownPreview`，不开 file tab——master file tab 只透传 filePath、embedded 路径磁盘不存在））。
- **单一启动门控（v2 关键决策）：贡献点语义校验只在前端 `registry.validate()` 做，后端不做 JSON Schema 校验。** 后端只做传输层检查（Web.Entry 非空 + 插件 ID 合法 + 静态托管路径安全）。禁止再引入后端贡献点 Schema 校验——会造成两处规则漂移（教训来自 dsh/cordis 的双重门控）。
- **PluginManifest.Web 字段是前端插件声明入口。** `plugin/plugin.go` 的 `Web *WebPluginDecl{Entry, Contributes(json.RawMessage)}`——Contributes 是**不透明 JSON 原样透传**给前端 runtime，后端不解析。web_plugin_list RPC（`serverapp/rpc_table.go`）只返回带 Web 声明的插件。
- **插件 web 产物静态托管路径 `/plugins/<id>/web/*`。** `channel/web/web.go` 的 `handlePluginStatic`：插件 ID 必须匹配 `^[a-zA-Z0-9][a-zA-Z0-9._-]{0,127}$`（`isValidPluginIDForServe`），子路径 clean 后必须落在 `<pluginDir>/<id>/web/` 内（防路径穿越）。`SetPluginDirs` 在 `serverapp/server.go` 注入 `plugin.DefaultPluginDirs(config.XbotHome())`。
- **前端插件激活路径 `activate`（URL import）/ `activateBuiltin`（静态 import）共用 `activateModule`。** 内置插件（xbot.plugin-manager、git-info、xbot.skill-manager）走 `activateBuiltin`，第三方插件走 `activate`（versioned URL `?v=<version>` 绕过浏览器模块缓存）。热加载 = 同 ID 先 `deactivate`（逆序执行 disposables + 移除贡献点）再激活新实例。
- **插件视图 entry 用 `builtin:` 前缀标记内置视图。** 内置视图有【两处】注册点，必须同步：① `usePluginRuntimeHost.ts` 的 `builtinViews` map（供 `loadBuiltinView` 解析，静态 import 组件）+ ② `PluginView.tsx` 的 `BuiltinView` switch（渲染分发 `case view.id`）。**只改 ① 漏改 ② 的症状：tab 存在但点击空白 + 无请求 + 无报错**（skill-manager 真实事故——switch 命中 `default: return null`，面板从未 mount）。第三方插件视图走 `/plugins/<id>/web/<entry>` URL import。新增内置视图必须：`builtinViews` 注册 + `BuiltinView` switch 加分派 + `builtin` 插件 manifest 声明 view 贡献点；守护：`PluginView.test.tsx` 断言每个 builtin view 渲染出内容。

- **⚠️ 插件会话身份协议（2026-09-06 "刷新后 git 面板显示不是 repo"根治）：`window.__xbot_session__` 是插件 RPC 注入 cwd 的唯一身份源，绝不能伪造兜底身份。** 布局恢复的插件 tab 可能**早于会话加载** mount（dockview fromJSON vs sessionStore activeSession 异步竞态）——git-fancy 旧 `resolveChat()` 在身份未知时回落 `{chatID:'default'}` → 后端 `web_plugin_rpc` 对 'default' `GetOrCreateSession` **创建幽灵会话** → 初始 workdir（非 repo）注入 cwd → 面板渲染"当前目录不是 git 仓库"且不自愈（关闭重开 tab 才恢复）。修复四件套：① `resolveChat()` 身份未知返回 **null**（git-fancy/shared.tsx，绝不伪造 'default'）+ `SessionNotReadyError`；② 面板 refresh/轮询/checkSessionKey 身份守卫（未就绪不发 RPC 保持 loading，session.switched 事件/3s 轮询重读身份自愈——checkSessionKey 身份 null 时**保持 lastSessionKeyRef 不变**，身份到达后 key 变化触发全量刷新）；③ **AgentPanel 的 `__xbot_session__` 写入空值跳过**（`progressChatID ?? ''` 的空 chatID 会毒化 usePluginRuntimeHost 设置的正确身份——resolveChat 对空值回落 'default'）；④ 后端 `GetOrCreateSession` 在 RPC 读路径上的幽灵会话副作用是前端伪造身份触发的前置条件（前端修复后不再触发，后端保持不变）。插件 web bundle 改动需 **esbuild 重建部署**（`npx esbuild src/plugins/git-fancy/{index,commit}.tsx --bundle --splitting --format=esm --jsx=transform --outdir=...` → `~/.xbot/plugins/xbot.git-fancy/web/`）+ **plugin.json version bump**（`versionedUrl(url, manifest.version)`——版本不 bump 则 `?v=` 不变，浏览器吃旧模块缓存）+ server 重启（重读 plugin.json）。Tests: `git-fancy/index.test.tsx`（身份未就绪零 RPC + session.switched 自愈）。
- **插件 editor-view API（VSCode webviewPanel 语义，§5.4）。** 插件可经 `ctx.ui.openViewTab({viewId, title, icon?, key?, params?})` 在主编辑区打开**参数化动态 tab**（如 git diff / commit 详情），`ctx.ui.openFileTab(path)` 复用宿主文件 tab。机制五件套：① `ViewContribution.dynamic: true`——动态视图不进 activity bar/侧栏/layoutRegistry（`usePluginViewPanels` 与 `syncViews` 都过滤），只能经 openViewTab 打开；② `PanelParams.viewKey`（dedup 键，`tabLogicalKey` → `plugin-view:${key}`，同 view 多实例/同 key 聚焦）+ `viewParams`（经 `renderPluginView` → `PluginView` → 插件组件 **props** 透传）；③ **plugin tab 的 dockview component 必须用 viewId**（`openTab` 里 `component: input.data.viewId ?? 'plugin'`）——`renderPluginView` 按 `view.id === component` 查找，传泛型 'plugin' 永远查不到（历史空白 bug）；④ 模块级桥 `plugin-runtime/editorTabs.ts`（PluginUI 在 React 树外，AppShell useEffect `registerEditorTabOpener` 注入 tabManager.openTab）；⑤ **多入口插件必须 esbuild `--splitting`**——activate(ctx) 在主入口注入 shared 模块的 rpc/ui 单例，单入口 bundle 会让每个 view entry 拿到独立副本（注入对其他 view 不可见）；共享 chunk 相对路径无 query → ESM 缓存同实例。参考实现 `xbot.git-fancy`（`web/src/plugins/git-fancy/` index/diff/commit/shared 四模块 + 后端 `plugins/xbot-git-fancy/main.go`：log skip/total 分页、commit 详情 numstat、diff 带 commit 参数）。**插件 view 组件的条件提前 return（loading/error 分支）之后绝不能再加 hook**——git-fancy 曾把 `expandedHash` useState 放在提前 return 之后，loading→loaded 切换时 hooks 数量 7→8 触发 React #310（Rendered fewer hooks than expected）整面板崩溃；复现测试 `git-fancy/index.test.tsx`（先注入 `window.React` 再**动态** import 插件模块——静态 import 会被提升到注入之前，shared.tsx 模块初始化读 window.React 拿到 undefined）。**插件 permissions 必须包含它实际用到的每个能力**——git-fancy 曾只声明 `["rpc"]` 而 `openDiffTab` 走 `ctx.ui.openViewTab`：buildContext 按权限注入（无 "ui" → ctx.ui undefined）→ 点击文件**静默无反应**（无报错无日志，极难排查）；守护测试 `plugins/xbot-git-fancy/main_test.go` `TestManifestPermissions`（Go 测试读同目录 plugin.json，比前端 vitest 读跨目录文件自然——web tsconfig 无 node:fs 类型）。**⚠️ 同一类回归已发生【两次】（2026-09-18 用户：「genui 插件之前不是加了分享功能吗？我现在怎么看不到分享按钮了」）**：分享 UI 的 `ctx.share`（`ShareablePanel` 的分享按钮 + `ctx.share.registerRenderer`）落地于 `37134718`，但 `plugins/xbot-genui/plugin.json` 的 `permissions` **从未声明 `share`** ⇒ 前端 `buildContext`（`web/src/plugin-runtime/context.ts`：`const has = (p) => permissions.includes(p)`；`if (has('share')) ctx.share = svc.share`）不注入该能力 ⇒ 插件里 `const button = share ? <button data-testid="genui-share"/> : null` **静默不渲染按钮、无任何报错**（只能靠读代码定位）。**修法 = 清单加 `"share"` + 同步【已部署副本】**（`~/.xbot/plugins/<id>/plugin.json` 与 `~/.xbot/plugins/builtin/<id>/plugin.json` —— 只改仓库文件**不会**影响运行中的宿主）+ `config action=reload_plugins`（重载后可用 `web_plugin_list` 回读 `permissions` 验证）。守护：`plugins/xbot-genui/manifest_permissions_test.go:TestManifestPermissions`（断言 `web.entry` 存在 + `share` ∈ permissions + UI 能力已声明）。**⇒ 规则：新增任何 `ctx.<capability>` 用法时，必须把对应 Permission 字面量写进该插件 manifest，并让同类测试断言它（否则失败是静默的）。****后端权限白名单必须与前端 Permission 类型同步**——`plugin/permissions.go` 的 `allPermissions` 曾只有 v1 权限（tools.register/ui.themes/...），前端 v2 的 6 个权限（events/commands/rpc/state/ui/plugins）只有 rpc 在白名单里 → manifest 声明 `"ui"` 被 reload 拒绝（`unknown permission "ui"`，前后端名单漂移）；新增前端 Permission 类型值时**必须同步** permissions.go 常量 + allPermissions 注册（PermRPC 的注释模式：`Matches the frontend Permission 'rpc' (web/src/plugin-api/manifest.ts)`）。白名单在 Go 二进制里——修复后需重启 server 才能用新白名单 reload。**stdio 插件二进制更新后需重载**：`/plugin reload-all` agent 命令或重启 server——`WatchConfig` **从未被调用**（grep serverapp/agent 无结果），改 config.json 的 disabled_plugins 不会触发重载（踩坑实录）。**测试 mock `usePluginRuntime` 必须返回稳定引用**——每次渲染返回新对象会让 `AsyncPluginView` 的 `useEffect [runtime]` 无限循环挂死 worker（表现：vitest 无输出超时）；用 `vi.hoisted` 定义稳定 `runtimeMock` 对象。
  **⚠️ 同一缓存同样适用于【清单本身】（plugin.json 的 `web.entry`/`web.i18n`/`permissions` 等）**：`PluginManager.Discover` 只在进程启动时读一次清单 ⇒ 之后改 plugin.json（含新加 `web.i18n`）**内存里不会变**，必须 `reload_plugins`（日志会打印 `Plugin discovered plugin=<id>`）或重启才生效。**不对称陷阱（2026-09-19 实测踩坑）**：插件的 web 静态产物 `/plugins/<id>/web/*.js` 是**每请求从磁盘读**的 ⇒ 会出现「前端 bundle 已更新、清单内容却没生效」的组合，症状是 `ctx.i18n` 全走兜底（宿主英文、插件仍中文）。排查手法：在插件面板打印一行诊断 —— `ctx`(模块级 ctx 是否被 activate 注入) / `i18n`(ctx.i18n 是否存在) / `locale` / `probe`(某 key 的实际解析值：命中=链路通、`<fallback-used>`=表没到、`<no-i18n>`=ctx 没到)；宿主侧同步打印 `hasTable`/`locales`/`hostLocale`。守护测试：`plugins/xbot-ssh-runner/manifest_i18n_test.go` + `web/src/plugin-runtime/toManifest.i18n.test.ts`。
- **InfoBar 渲染 fancy git 视图 + 排除旧 git 文本 span。** `InfoBar.tsx` 同时渲染 `PluginPanelContainer(container="info_bar")` 和 `WidgetZone(zone="infoBar", excludePrefixes={['git:']})`——避免 git 状态双渲染。`WidgetZone` 新增 `excludePrefixes` prop。AppShell 布局测试 mock 掉 `PluginPanelContainer`（布局测试不测插件面板）。
- **插件能力即类型（`PluginContext<P>`）。** 权限列表 `P` 决定 ctx 上哪些能力接口可用，未声明的是 `never`（编译期禁止）。运行时 `buildContext` 按权限注入（未声明的不存在）。禁止在运行时做 Proxy 白名单拦截——权威在编译期（无沙箱设计，信任 = 安装）。
- **Ambience Layer（氛围装饰层，四层模型）。** 宿主文件 `web/src/ambience/`（store.ts 单例 + AmbienceRoot.tsx + wallpapers.ts + api.ts）。四层：wallpaper z:0（AmbienceBackground，AppShell/MobileAppShell 根容器**第一子元素**）→ app 玻璃化 → decoration z-30 / hud z-[35]（AmbienceOverlays，根容器**最后子元素**，在 FloatingLayer z-40/Dialog z-50 之下——装饰绝不遮挡浮层/对话框）。关键决策：① **玻璃化 = CSS 变量覆盖法**——AmbienceBackground 覆盖 `--bg-primary/secondary/tertiary` 为 `color-mix(in srgb, 原色 N%, transparent)`（inline style on `<html>`，cleanup removeProperty 恢复；主题切换经 effect deps `theme` 重算——先恢复再读新值）；**不要**给散布的 bg 消费点逐个加 backdrop-filter。② **overlay 归属插件**：`index.ts` activate 时 `new PluginUI(this.host.ui, effective.id)`（per-plugin 实例）——PluginUI.mountOverlay 第一参 pluginId 追踪归属；`syncPluginWallpapers` 全量 diff 时清理已卸载插件的 overlay 残留（usePluginRuntimeHost 的 syncAmbience effect，订阅 subscribeViews 复用插件激活/卸载通知）。③ **壁纸上传 IndexedDB 本机**（`xbot-ambience` DB，canvas 压缩 ≤1600px + tone 亮度采样；GIF 不压缩原样存 ≤4MB）；配置（profile JSON）走 `user_settings` 跨设备同步（localStorage `xbot:ambience` ↔ server `web:ui:ambience`）——资产不同步，其他设备回落插件预设。④ **会话级 profile**：`setSessionProfile(chatID, patch|null)`（null 删除回落全局）；AmbienceBackground 经 useSessionStore.activeSession → `ambienceStore.setActiveSession` 跟踪会话切换。⑤ `AmbienceContribution.wallpapers` 是 `readonly WallpaperPreset[]`（支持插件 `as const` satisfies 声明）。⑥ registry kind 白名单已含 `'ambience'` + `listAmbience()`（照 listAllViews 模式）。官方示范插件 `web/src/plugins/xbot-ambience/`（AmbienceContribution 壁纸包 CSS 渐变 + 状态桌宠 PetWidget + Particles 星尘）——usePluginRuntimeHost `activateBuiltins` 第三个注册；桌宠情绪机 `turn.started→thinking / progress.iteration→working / turn.ended→done|sad / 闲置10min→sleeping`，`pet.setProps` 更新（mountOverlay 返回 handle）。设置面板：SettingsAppearance 的 `AmbienceSettings` 区（壁纸网格/上传/玻璃滑杆/会话 profile 开关；`resolveWallpaper(id).css` 直接作缩略图 background）。
- **⚠️ Ambience store 高频 emit 风暴（2026-08-29 桌宠事件事故，三条防御缺一不可）。** 根因链：`progress.iteration` 事件（agent 运行时高频）→ 桌宠 `setMood` 无 guard → `store.setProps` 无 diff → `emit` → `buildSnapshot` 重建（`wallpapers: flatMap` **每次新数组引用**）→ `AmbienceBackground` 的 `wp` useMemo deps `snap.wallpapers` 失效 → 玻璃化 effect deps `wp`（**对象引用**）失效 → 每条事件重跑 `getComputedStyle` ×3（强制同步样式计算）+ `setProperty --bg-*` ×3 + cleanup `removeProperty` ×3（**两轮全站 CSS 变量失效**）→ layout/paint 风暴（trace：5 秒 2925 RunTask/s + UpdateLayoutTree ×375 + animationcancel ×107），git 插件 RPC 饿死（面板永远"加载 git 状态…"）。三条修复（`ambience.test.ts` + `xbot-ambience/index.test.ts` 回归测试守护）：① **store `setProps`/`show` 浅比较 diff**——相同内容不 mutate 不 emit（事件源头节流）；② **snapshot 集合引用稳定**——`wallpapersArr`/`userWallpapersArr` 按数据源身份缓存（`wallpapersSrc !== state.pluginWallpapers` 才重建），无关 mutate（overlay setProps）不换引用；`userWallpapers` Map 必须**不可变替换**（`new Map(old)` 后 set/delete——可变操作不换身份，快照漏更新）；③ **effect deps 只吃原始值**——玻璃化 effect deps 用 `wpCss`（resolveWallpaper 的 css 字符串）+ `opacity`/`blur`（解构后的 number），**绝不放 `wp` 对象**（resolveWallpaper 每次新对象）+ 插件侧 `setMood` 的 `curMood === mood` guard（相同情绪直接 return）。**教训：模块级 store 的高频事件路径（progress/stream）上，任何 emit 都会引爆全订阅者重渲染链——setProps 必须 diff、snapshot 集合必须身份稳定、effect deps 必须原始值。**
- **⚠️ Ambience 桌宠事件源 = SSE 桥（emitPluginEvent），不是 web_plugin_event。** 插件订阅的 `turn.started`/`turn.ended`/`progress.iteration` 事件的真实源是 **useProgressStream 的 SSE 分支**（模块级事件桥 `plugin-runtime/events.ts` 的 `emitPluginEvent`——`PluginRuntimeProvider` 挂载时 `setActiveEventBridge(runtime.events)` 注册，useProgressStream 四处置入：turn_started 分支（turn.started）/ text 完成+cancelled（turn.ended ok/cancelled）/ progress_structured 非 turn_started（progress.iteration——p.active_tools 作 tools payload）。**不要假设 EventMap 有事件就有人 emit**——`web_plugin_event` WS 消息只转发后端插件事件（turn 生命周期是前端 SSE 域）。无 Provider 时 emit 静默丢弃（单测/降级安全）。
- **⚠️ Ambience 渲染静态成本三禁令（2026-08-29 第二次性能事故——React emit 频率修复无效后的真根因）。** 三层 emit 防御（上一条）只砍 React 更新频率；**真正的杀手是每帧渲染管线静态成本**（CSS 动画 + GPU 合成不依赖 React——React 零更新也全速跑）：trace 证据 285 帧全速（Paint ×390 > 帧数 = paint 属性动画在跑）+ GPUTask 626 + Layerize 285。**装饰组件三禁令**：① **禁 paint 属性动画**——`transition: 'box-shadow'` 是 paint 属性（逐帧插值 = 每帧重绘），合成友好动画只允许 opacity/transform（PetWidget 的 glow 改静态 box-shadow）；② **禁 backdrop-filter**——backdrop-blur 每帧重采样背后内容（背后有动画 = 链式重算），玻璃感用纯色半透明 rgba；③ **全屏 filter: blur 默认关**——壁纸柔焦（DEFAULT_GLASS.blur=0）是 GPU 大纹理每帧重合成大户（用户显式调滑杆才开）。**星尘粒子（Particles）默认不挂载**——34 星 box-shadow glow + twinkle 动画的 Layerize/光栅化静态成本 + z-30 全屏盖在 dockview 插件面板上（用户报告"干扰别的插件渲染"——decoration 层语义是内容之上，粒子画在面板上不是氛围是遮挡）。粒子能力保留在 Particles.tsx（未来插件配置可选启用：低数量 + will-change 合成层 + 无 box-shadow）。
- **插件管理面板显示内置插件（activateBuiltin 的可见性）。** PluginManagerPanel 的数据源是后端 `plugin_status` RPC（Go 插件）——activateBuiltin 的**前端内置插件（plugin-manager/skill-manager/ambience）不在列表**（用户报告"写的不是真插件列表看不到"）。修复：refresh 合并 `runtime.listPluginStates()`（前端 registry 差集 + `builtin: true` 标记）——builtin 行显示"前端"徽章 + 后端操作（toggle/reload）不适用（无 Go 实例，显示"前端运行时 · 随宿主启动"）。
- **EventMap / BackendRPC / ToolResultMap / PluginExportsMap 都用声明合并扩展。** 后端插件发布 `.d.ts` 类型包，前端插件 `import` 后获得跨插件类型化契约。空接口（如 `PluginExportsMap {}`）是声明合并的合法扩展点，eslint 用 `@typescript-eslint/no-empty-object-type` disable 注释标记。
- **Web 插件面板 UI 统一（关键架构）：插件 view 贡献点声明一次，桌面 + 移动两端侧栏自动出现面板 tab，绝不允许再在 RightSidebar / MobileAppShell 里硬编码 `case 'xxx'`。** 机制：`registry` 维护 view 集合订阅（`subscribeViews`/`listAllViews` + `notifyViewsChanged`，registerPlugin/unregisterPlugin 时触发）→ `usePluginViewPanels(container)` hook（`useEffect`+`useState` 订阅）返回某容器下插件 view 面板列表（含 pluginId/title/icon/container）→ 两端 tab 列表 = 内置面板（files/search/info/tasks/terminal）+ `usePluginViewPanels('right_sidebar')` 动态追加。渲染用 `PluginView`（`plugin-runtime/PluginView.tsx`，含 `PluginViewErrorBoundary`——插件视图崩溃只塌陷该 tab 不黑屏整应用）。`SidebarPanel` 类型已从硬编码 union 改为 `string`（内置面板名或插件 view.id）。图标用 `pluginIcon(name)`（`pluginIcons.ts`）把 view.icon 字符串映射到 lucide。
- **VSCode 式布局定制系统（`web/src/plugin-runtime/layoutTypes.ts` + `layoutRegistry.ts`）：命名布局槽位 + 用户/插件可移动 UI 元素。** 核心：`LayoutSlotId`（mobile.bottom_nav / mobile.top_bar / desktop.activity_bar / desktop.sidebar / desktop.info_bar / desktop.main）→ `LayoutItem`（id/slot/title/labelKey/icon/weight/movable/**group**）注册到 `layoutRegistry`（单例）→ `itemsFor(slot)` 返回该槽位实际项（默认 + 用户覆盖，按 weight 升序）→ `useLayoutItems(slot)` hook 订阅响应式渲染。用户覆盖**后端权威**（`userSettings.ts`：localStorage key `xbot:layout:overrides` ↔ server key `web:ui:layout-overrides`，`syncAndMigrateSettings` 换浏览器/设备拉回 + `syncSettingToServer` debounce 写回 user_settings 表，`SETTINGS_SYNCED_EVENT` 触发 layoutRegistry 重载），`moveItem`/`resetItem`/`resetAll` 操作。**内置项**：`registerBuiltinLayoutItems()` 在 App.tsx 模块级注册（会话/工具/新会话/设置 + 桌面侧栏五项）；**插件 view 自动注册**：`usePluginRuntimeHost` 的 PluginRuntimeBootstrap 同步 `runtime.listAllViews()` → `layoutRegistry.register`（container → 默认 slot 映射 `VIEW_CONTAINER_TO_SLOT`，weight 100 排内置项后），插件卸载/热加载时 diff 注销消失项。**消费端**：MobileAppShell 底部导航/顶栏改 slot 驱动（`useLayoutItems('mobile.bottom_nav'/'mobile.top_bar')` + `renderBottomNavItem`/`renderTopBarItem` 按 item.id 分派动作，插件项打开工具页对应 tab）；桌面 RightSidebar/RightActivityBar 过滤掉被移出 desktop.sidebar 的项。**设置 UI**：`SettingsLayout`（SettingsDialog「布局」分类，i18n `settings.nav.layout`）列出全部项 + 目标槽位 select + drag 拖拽移动（拖一项到另一项上 = 移到该项所在 slot，native HTML5 drag）+ 单/全部重置。**主视图 tab（container=`main`）**：`ViewContainer` 增 `main`，`VIEW_CONTAINER_TO_SLOT.main = 'desktop.main'`。插件声明 `container:'main'` 的 view 成为 Dockview **主编辑区 tab**（VSCode editor 语义，全宽渲染 diff 等）：`DockviewContainer` 的 `ReactContentRenderer.render()` 在 `CONTENT_COMPONENTS` 查不到内置 panel 时回退到 `PluginView`（component name === view.id）；`RightActivityBar` 底部渲染 `usePluginViewPanels('main')` 的图标，点击经 `onOpenMainView` → `tabManager.openTab({type:'plugin', data:{viewId, pluginId}})` 打开。`useTabManager` 支持 `TabType='plugin'`（openTab/panelToTab/tabLogicalKey 去重 by viewId）+ `getLayoutJSON/applyLayoutJSON`（封装 dockview `toJSON/fromJSON`）+ `filterPanels`（按 predicate 递归过滤 grid 树：leaf group 移除 view、空 group 折叠、branch 空子移除、单子提升——`filterAgentPanels` 过滤 closable=false 主 agent、`filterTerminalPanels` 过滤 PTY 禁用的 terminal）。**`useLayoutPersistence` 用完整 dockview 布局序列化**（`getLayoutJSON` + `filterTerminalPanels` → localStorage，恢复 `applyLayoutJSON`/fromJSON），支持**多组 grid 分割、多实例 panel 位置/尺寸**的完整恢复；常驻 agent tab 完整保留（无 sessionId，AgentPanel 读 activeSession 动态）。**左侧 VSCode 式 section 堆叠（SidebarSectionStack）**：AppShell 左侧 = `SidebarSectionStack`（`web/src/components/sidebar/SidebarSectionStack.tsx`），会话列表 + 每个用户移入 `desktop.activity_bar` slot 的插件 view 渲染为**独立 section**（不再是插件 tab 切换，`useLeftPluginPanels` hook 取列表）；相邻 section 间**拖拽分隔条**调整上方 section 高度（像素级，min 80px）；高度与折叠状态**纯前端持久化**（localStorage `xbot:leftbar:section-heights` / `xbot:leftbar:section-collapsed`）；未拖过的 section 按 `defaultHeight` 有无自动分配（插件 240px 固定、会话 flex 占剩余）——自动 layout 与手动拖拽共存。**每个 section 确定高度约束 + overflow-hidden，根治「会话列表自然高度溢出覆盖下方插件区」的重叠 bug**（旧结构外层容器非 flex 无裁剪 + CollapsibleGroup 高度由内容决定；CollapsibleGroup/LeftActivityBar 组件已删除）。`layoutRegistry` 的 group 折叠 API（`isCollapsed/setCollapsed/toggleCollapsed`，`xbot:layout:collapsed`）仍存在但左侧栏不再使用（SidebarSectionStack 用自己的 per-section 折叠）。⚠️ 布局项文本走 `labelKey` i18n（优先于 title），内置项 labelKey 必须用真实存在的 i18n key（如 `sidebar.sessions`/`agent.tools`）——`session.view`/`session.tools` 不存在会显示原始 key。测试：`layoutRegistry.test.ts` 12 个（含分组折叠持久化）+ MobileAppShell.test 固定 zh-CN locale（`changeLocale('zh-CN')` + 中文断言，底部按钮无 aria-label 避免与顶栏 getByLabelText 冲突）。**排序系统（VSCode 式拖拽重排）**：`LayoutOrder`（slot → 有序 itemId 列表，`LAYOUT_ORDER_KEY = 'xbot:layout:order'` ↔ 后端 `web:ui:layout-order`，经 SETTING_MAP 自动同步 user_settings 表）；`itemsFor` 排序 = order 索引优先、未记录项按 weight 追加在后（新装插件稳定落尾）；`moveItemTo(id, slot, {beforeId})` 按**移动时完整顺序快照**写 order（被移项落在用户看到的真实位置——只记单项会让它因"已排序项优先"排到最前，真实事故）；`setSlotOrder(slot, ids)` 整槽重排（去重、保留未知 id 作位置记忆）；`resetItem/resetAll` 连 order 一起清。**真实 UI 拖拽**：SidebarSectionStack header（slotId prop 启用，AppShell 按 `useLayoutItems('desktop.activity_bar')` 顺序建 sections，sessions section id = `desktop.activity.sessions`，高度 key 一次性迁移）+ RightActivityBar 图标（统一按 layoutItems 顺序渲染内置+插件）都支持 HTML5 DnD：dragStart 记源 id 到 state（dragOver 读不到 dataTransfer）、指针相对目标 header 中线判 before/after、`computeReorder`（`web/src/lib/reorder.ts`，no-op 返回 null 不持久化）+ 插入线指示（`data-testid="insertion-line"`）。SettingsLayout drop 改 `moveItemTo(beforeId)`（拖到项上 = 插到该项前，与真实 UI 语义一致）。**后端只存 slot 归属 + ordering 两个 key（`web:ui:layout-overrides`/`web:ui:layout-order`）；宽度/高度/折叠等细节纯前端 localStorage。** **E2E 面板数断言（`e2e/right-sidebar.spec.ts`）：右侧栏 `button[aria-pressed]` 数 = 5 内置（files/search/info/tasks/terminal）+ right_sidebar 内置插件数（当前 plugin-manager + skill-manager = 2，共 7）。内置插件无条件 `activateBuiltin`（不依赖后端，standalone 也是 7）；新增 right_sidebar 容器内置插件时必须同步更新此断言（历史事故：skill-manager 加入后 6→7，`toHaveCount(6)` 红灯）。本地跑 E2E 需真实后端登录（standalone dev server 无后端会卡在登录页 `Invalid username or password`，非断言问题）。**
- **⚠️ 内置插件视图必须静态 import，禁止动态 `import()` 加载（React #311 黑屏根因）。** `PluginManagerPanel`/`GitStatusPanel` 这类内置视图随主 bundle 分发，用动态 `import('@/plugins/...')` 会让 rolldown 把它们打成独立 chunk，并在符号提升时把 `useState`/`useEffect`/`useCallback` **错误绑定到 `vendor-framer-motion` 的导出**（chunk 头出现 `import{i as t,r as n}from"./vendor-framer-motion..."`，然后 `i.useState` 被调用）→ React 报 `Minified React error #311`（hooks 数量不一致）→ 整个前端黑屏。修复：`builtinViews` 从 `Map<string, () => Promise<Component>>` 改为 `Map<string, Component>`（静态 import 组件），`loadBuiltinView` 同步返回。只有第三方插件（`/plugins/<id>/web/` 的 URL）才走动态 import。排查线索：黑屏 + 错误栈含 `framer-motion` 的 `useState` + 某个组件 chunk 名，即此根因。
- **⚠️ 手机端设置 Sheet（SettingsDialog）必须做 `sm:` 断点适配——桌面类无断点会挤爆手机内容区（2026-08-29 "手机 LLM 配置 tab 超出屏幕"修复）。** MobileAppShell 直接复用桌面 SettingsDialog（`w-[720px] max-w-full` Sheet + `w-36` 竖直侧栏）：手机 375px 下 Sheet 正确收缩 100vw，但 **w-36 (144px) 侧栏占掉 38% 屏宽 → 内容区仅 230px**，LLM 控制台 header 工具按钮组（`shrink-0` 281px 单行）溢出内容区 67px（rect.right=442 > 视口 375）。修复三件套（`SettingsDialog.tsx`/`SettingsLLM.tsx`）：① 手机 nav 横向化——外层 `flex-col sm:flex-row` + nav `w-full flex-row overflow-x-auto border-b sm:w-36 sm:flex-col sm:border-r`（横向滚动 tab 条，桌面竖直侧栏不变）；② flex 子项固定宽超容器时 **`shrink-0` 是溢出元凶**——LLM header 按钮组 `shrink-0` → `min-w-0`（允许收缩 + 内部 flex-wrap 换行）；③ **`translateX(105%)` 隐藏 drawer 会给父容器留 5% 的 scrollable overflow**（CSS Overflow Module：transform 后位置计入 scrollWidth → 内容区横滚 18px 空白）——改 `translateX(100%)` + 内容区显式 `overflow-x-hidden`（表单面板无横滚场景）。E2E 守护：`e2e/settings-llm-mobile.spec.ts` 双场景（375px 手机：内容区全宽 ≥90% + `overflow-x hidden` + 无可见溢出元素——探测排除隐藏态 transform/visibility + 合法横滚容器 nav tab 条；1280px 桌面：Sheet 720px + nav 竖直 144px 回归保护）。**教训：E2E 溢出探测必须区分"合法滚动"（overflow-x auto 容器内）与"布局溢出"（无滚动容器的 rect 越界）——nav 横滚 tab 条的 right=1079 是特性不是 bug。**
- **⚠️ 一个会话至多被一个 agent 面板渲染（session-per-tab 不变量）——「切会话后同一 user 行重复渲染」根治（2026-09-16）。** 现象：切到 S2 后 `hello from S2` 在 DOM 命中 **2 个**元素、两行 `data-message-id` 完全相同（`db-3`/`turn-202-c`）、`/api/history` 对同一会话拉**两次**、SSE 双订阅。根因不是 reducer 重复造行，而是**两个 `renderer:'always'` 的 agent 面板同时挂载同一会话**：① seed 在“还没有任何已知会话”时建的 agent tab **没有 sessionId**（`DockviewContainer.tsx` 的 seededRef 分支），而 `AgentPanel` 的会话解析是 `params.sessionId ?? activeSession?.chatID`（占位 tab 跟着 activeSession 走）；② 侧栏点击会话（`SessionSidebar.handleSelect` 桌面路径）既 `tabManager.openTab({type:'agent', data:{filePath:id}})` **又** `store.activateSession(id)` ⇒ 占位 tab 与新建的 session tab 渲染同一会话。agent tab 常驻 DOM（renderer='always' 保虚拟列表，见 useTabManager.openTab 注释），因此整棵消息列表渲染两份。**修复（两处，互为独立护栏）**：① `useTabManager.openTab`——为会话打开 agent tab 时**认领**未绑定会话的占位 tab（改绑 `sessionId/channel/title` + `setTitle`，不新建面板；占位 tab 的语义就是“首个会话的槽位”）；② `AgentPanel`——占位 tab 仅在**独占** main agent 面板时才跟随 `activeSession`（`containerApi.panels` 里已有别的 panel 拥有该会话 ⇒ 渲染空态而不是镜像；订阅 `onDidAddPanel/onDidRemovePanel` 保持响应）。**守卫**：`web/src/hooks/useTabManager.test.ts`（认领 2 例：复用占位 + 不同会话仍各开面板）+ `web/src/workspace/panels/AgentPanel.test.tsx`（不镜像 + 独占时仍跟随）+ `web/e2e/session-switch-user-message.spec.ts`（**契约：任何行 id 不得在 DOM 出现两次** + 切一次的目标会话 history 只拉一次）。mutation 自证：关掉①⇒2 个单测红；关掉②⇒不镜像用例红；两个都关⇒e2e 两用例红（Received 2）。**⚠️ E2E 断言坑**：`[data-message-list-content]` 个数**不等于 1** 是对的——session-per-tab 下每个会话 tab 各有一个常驻列表（renderer='always'），所以“列表根个数”随 tab 数增长是设计；缺陷判据是**同一行 id 出现两次**（用 `evaluateAll` 跨 tab 收集 `data-message-id` 查重），不要写“列表根恰好 1 个”。**
- **⛔ 占位 agent 面板【不承载会话时不得渲染任何面板 UI】——「切换会话闪烁一瞬间错误布局」的真因（2026-09-18，帧级 E2E 定稿）。** 现象（用户截图）：主区消息区**上方浮着一排输入框控件**（回形针 / ContextRing / 蓝色发送按钮），随切换会话闪现。帧级证据（`e2e/session-switch-loading.spec.ts` 给每个 agent 面板打印归属 + 矩形）：切换瞬间**同时存在两个 agent 面板** —— `(seed)|vis=1|rect=330,36 950x644` 与 `chat-1|vis=1|rect=330,680 950x680`（先上下分屏），随后两者**完全重叠**。根因：占位 tab（无 sessionId，见上一条）在**已有别的面板承载 activeSession** 时 `chatID=null`（不镜像 —— 这是对的，避免同一会话渲染两份），但它**仍然渲染整块面板 UI**（欢迎空态 + `MessageInput`；`MessageInput` 只判 `!isSubAgent`，没有 chatID 守卫）⇒ agent tab 是 `renderer='always'`、dockview 切换瞬间又重排分组/尺寸 ⇒ 这些**没有任何会话可承载**的控件漏进可见区（loading 屏是**透明背景**，遮不住）。修法（`AgentPanel`，必须在**所有 hook 之后**、主 return 之前）：`if (isPlaceholderMainAgent && sessionOwnedByPeerPanel) return null`（tab 本身保留，尺寸归 dockview）。⛔ 不要用「给 loading 屏加不透明背景」遮掩 —— 要修的是「无会话的面板不该渲染 UI」。守护：`session-switch-loading.spec.ts` 断言 `document.querySelectorAll('[data-agent-chat-id=""]').length === 0`（幽灵面板漏出输入框控件时必红）。
- **⛔ 不可见面板不得在 DOM 里保留 chrome（2026-09-19 P0：切回「已打开过」的 tab 时输入框浮在消息区上方一闪）。** 用户复现条件是决定性的：**只有电脑端、且目标 tab 之前已经打开过**时出现；先把该 tab 从 tab 栏 x 掉再切就正常。⇒ 已打开 = 面板早已挂载（`renderer:'always'` 常驻 DOM）。dockview 对非激活面板的 overlay 置 `visibility:hidden`，激活那一帧在它**自己的 rAF** 里清掉 hidden，而 React 的 `isVisible` 更新（+ `markHistoryStale` ⇒ loading 屏）落在**更晚的提交** ⇒ 浏览器先画**陈旧 DOM**。旧代码只把 `MessageList` 按 `isVisible` 隐藏，**托盘/输入框仍在 DOM** ⇒ 画出「空消息区 + 托盘 + 输入框」（输入框自然高度、贴在面板顶部）= 用户截图那排浮着的控件（回形针/Clock/Stop）；新建 tab 无陈旧 DOM（首帧就是 loading 屏）⇒ 「先 x 掉再切」就正常。**契约：不可见 ⇒ 消息区、托盘、输入框一律不渲染**（`{!isSubAgent && isVisible && !(sessionLoading || switchSplash) && …}`）；草稿不丢（MessageInput 每次击键即时写 `localStorage['xbot:draft:<sessionKey>']`，重挂载即恢复）。守护：`AgentPanel.test.tsx` 的「不可见面板不得在 DOM 里保留 chrome」（可见时标记存在 → 置 `isVisible:false` 后必须消失；`MessageInput` mock 渲染 `data-testid="agent-composer"` 才能断言存在性）。**新增任何 panel chrome 时按同一规则门控 `isVisible`。**
- **飞书进度渲染 = 飞书原生 CoT（「思考过程」），对齐 dsh-lark（2026-09-16 用户要求：「飞书模仿 dshlark 改成飞书原生 cot，飞书的渲染功能和 dshlark 对齐」）。** 契约照抄 `omdsh-dev/dsh-lark`（`src/runtime.ts` + `src/cot.ts`）：① 建思考过程 `POST /open-apis/im/v1/message_cot?receive_id_type=chat_id`（`{receive_id, origin_message_id?, cot_hidden, enable_badge:false, update_feed_rank:false}` → `{cot_id, message_id}`）；② 写事件 `PUT /open-apis/im/v1/message_cot`（`{events:[{event_type, content(JSON 字符串), timestamp}], message_id, cot_id}`）；**单次 ≤50 事件、单事件 content ≤4096 字符（rune 安全截断 + `{"truncated":true}` 标记）、timestamp 必须严格递增**（客户端按它排序，重复即乱序）。事件族 = AG-UI：`RUN_STARTED / REASONING_MESSAGE_{START,CONTENT,END} / TOOL_CALL_{START,ARGS,END} / TOOL_CALL_RESULT{content:{type:'code',code}} / RUN_FINISHED / RUN_ERROR`；工具图标词表 `read/write/search/bash/default`；工具结果按 **code block**（≤1500 rune，rune 安全）。**答案不进 CoT** —— 最终答复仍走普通消息（平台语义；dsh-lark 的 answer renderer 同理），思考区只承载「推理 + 工具调用 + 工具结果」。实现：`channel/feishu/feishu_cot.go`（传输：懒创建 + 单 drainer **异步**批写，**绝不阻塞 agent loop**）+ `channel/feishu/feishu_cot_renderer.go`（结构化进度 → AG-UI 事件）。⚠️ **xbot 的流式推送是全量**（`engine_wire` 的 delta_push=false）⇒ reasoning 必须**自己做增量差分**（`cotDelta`）再写 `REASONING_MESSAGE_CONTENT`，否则思考区把整段推理重复 N 遍；文本被改写（非前缀扩展）时整段补一次，绝不静默丢内容。开关：`FeishuConfig.Output`（`"cot"` = 原生 CoT；其他 = CardKit 卡片），生产由 `serverapp/feishuChannelOutput` 默认 `"cot"`，也可用 `channels.feishu.output` 覆盖（schema 已加该字段）；**测试直接构造渠道时为零值 ⇒ 仍走卡片路径**（既有卡片契约测试不受影响）。CoT 创建/写入失败即标 broken 并**降级到 CardKit 卡片**（进度是呈现，答案从不依赖它）。Tests: `channel/feishu/feishu_cot_test.go`（请求形状与方法/事件类型序列/时间戳严格递增/content 4096 截断与标记/全量→增量差分/图标词表/结果 code block 上限/broken 降级）。**（2026-09-16 用户追加：「cot 要尽可能好看，包括 tool、content 展示的方式都要和 dsh lark 统一」）** 追加对齐两项 —— ① **工具标题 = 「描述这次调用在做什么」的单行标签**（dsh-lark `PresentedCall.title` 的原话：「Short, always-visible label describing what THIS call does」，**不是工具名重复**）：`cotToolTitle` 按工具语义从 `Args`(JSON) 取最有信息量的字段（Shell→`command`、Read/Write→`path`、Grep→`pattern`、Fetch→`url`、SubAgent→`role`、`task_*`→`task_id`…），单行 + rune 截断 96，无参数才回落 Label/Summary 首行、最后才是工具名（`cotPrimaryArg`/`cotArgKeys`/`cotBoundTitle`）；② **正文的 hold/supersede 规则**（dsh-lark：「the text this one replaces was narration, not an answer: it belongs in the process」）：**只有最后一次 assistant 文本是答案**（走普通消息），被顶替的中间文本 flush 进思考过程 `TEXT_MESSAGE_{START,CONTENT,END}`（`messageId=text-<turn>-<n>`、`role=assistant`），flush 时机 = 迭代推进 / 工具调用开始（与「思考块不跨工具调用」同判定）；③ **不写 STEP 事件**（dsh-lark 注释：迭代编号对读者无信息量）。对齐核验方式：`grep -o "cotEvent('[A-Z_]*'" dsh-lark/src/cot.ts` 的事件集合与本实现 `emit("[A-Z_]*")` 的集合**差集必须为空**（当前 13 类完全一致）；常量亦须一致（MAX_EVENTS_PER_WRITE=50、MAX_EVENT_CONTENT_CHARS=4096、结果 boundResult=1500）。守卫：`TestCotToolTitle_CarriesPrimaryArg`（6 组参数优先级 + 单行 + rune 截断）与 `TestFeishuCoTRenderer_NarrationFlushedOnce`（narration 恰好 flush 一次；**最后一次答案不得进 CoT**）。

- **⛔ 飞书原生 CoT 五个渲染根因（2026-09-17 用户连续四次真机复查后定稿，每条都有红灯→绿灯的判别力测试）**：① **工具身份必须用宿主的 `protocol.ToolProgress.CallID`**（= dsh-lark 的 `event.data.callId`），**绝不能用「名字#迭代号#(Label|Args)」拼** —— 工具完成时 `updateToolResultLine` 会用 `formatToolProgress(Name, Arguments)` **重算 label**，而 active 快照里 label 还是空的 ⇒ 同一次调用得到两个 key ⇒ `TOOL_CALL_START(idA)` + `TOOL_CALL_RESULT(idB)` 被平台算成**两次调用**（用户截图：web 上只调了 1 个 FileCreate，飞书显示 `Called tools 2 times`，同轮 Shell 全部正常）。守护 `..._SameCallCountsOnceWhenSnapshotFieldsDrift`（回退即红：starts=`FileCreate#1 {args}` vs results=`FileCreate#1 /tmp/x.py`）。② **推理 messageId 必须【每轮一个】`reasoning-<turn>`**（dsh 同）：平台按 id 归并内容 ⇒ 工具开始后**迟到的推理尾巴**用同一 id 再 START 会并回同一块。曾改成"每块独立 id" ⇒ 尾巴成**新块** ⇒ 飞书里「推理停在工具调用那一刻（**不是推理结尾**）+ 位置错乱」（用户截图 + DB 真值：该迭代推理 1864 字符，飞书只渲染到 `Let me create /tmp/ems_weather.py`）。守护 `..._ReasoningTailAfterToolMergesIntoOneBlock` / `..._ReasoningIsOneBlockPerTurn`。③ **事件必须按时间片合并（`cotTextFlushInterval`=250ms）+ 长文本分片（`cotTextChunkRunes`=1024 rune）**：引擎**每个 LLM chunk 回调一次**（一轮上千次），逐 chunk 写会把一轮放大成上千个 CoT 事件 ⇒ 平台侧「**只有第一个迭代渲染，后面迭代都不渲染**」（日志实证：全程 `code=0`、无任何写失败/平台拒绝 —— 所以不是我们的熔断，而是事件洪峰）；且单事件 content >4096 会被传输层换成 `{"truncated":true,…}` 标记 = **毁内容**，长文本必须**分片**（对齐 web SSE 的 stateless 合并 + 推送频率控制）。守护 `..._CoalescesReasoningEvents`（200 次回调 ⇒ ≤5 事件且内容完整）+ `..._EventOrderIsLinear`（**线性一致性**：合并只改频率、绝不改顺序；单队列→单写线程→时间戳严格递增）。④ **瞬时写失败绝不允许熔断**：旧 `drain`/`flushNow` 一次失败就 `pending = nil` + `broken = true` ⇒ 该 turn 后面的思考过程**全部静默丢弃**（用户：「后面的 cot 都不渲染」）；现同批**重试 3 次**（失败批放回**队首**，保证顺序与内容都不丢），仍失败才降级并把 `dropped_events` 写进日志（绝不静默）。守护 `TestFeishuCoT_TransientWriteFailureRetriesInsteadOfDropping`。⑤ **正文必须落在它自己的工具之前**（用户：「第一个迭代的 content 渲染在第一个迭代的 toolcall 之后」）：旧实现只在"被更新的文本顶替"时才写 narration ⇒ 迭代 1 的正文要等迭代 2 的正文到达才落盘，于是排到迭代 1 的工具之后（web 上是正文在前）。**工具出现 = 这段正文是过程叙述而非最终答案 ⇒ 立刻写 `TEXT_MESSAGE_*`**；最终答案之后没有工具 ⇒ 一直 held、绝不进思考区（由普通消息发送）。守护 `..._NarrationRendersBeforeItsOwnTool` / `..._NarrationWrittenExactlyOnce`（工具的多次状态快照绝不能让同一段正文写多遍）。

- **⛔⛔ AskUser 面板「完全不弹窗」的根因（2026-09-17 用户 P0 实测；确定性 bug，不是偶发）**：WaitingUser 出站消息的**派发判据必须用会话/进程级 ctx，绝不能用 per-request 的 `reqCtx`**。完整链路：`chatProcessLoop` 里 `reqCtx, reqCancel := context.WithCancel(ctx)`（agent.go:3349）→ 请求体在匿名函数里跑，**defer 的 teardown** 调 `finishActiveCancelState`（:3402），而该函数结尾**无条件 `reqCancel()`**（:1620，为释放资源）→ 但 **WaitingUser 派发发生在 teardown 之后**（:3490-3514）却仍 select `<-reqCtx.Done()` ⇒ **「发送到 bus」与「ctx.Done」两个分支同时就绪 ⇒ Go 随机选** ⇒ WaitingUser 被随机丢弃 ⇒ 浏览器收不到 `WaitingUser:true` ⇒ **AskUser 面板不弹**（用户只看到一个 ✓ 的 AskUser 工具 pill + turn 直接结束），且丢的那次**连 `ask_question` 都没落库** ⇒ 刷新也恢复不了。**日志指纹**：`Tool done elapsed=0s tool=AskUser` + `Tool is waiting for user response, sending WaitingUser outbound` + `Context cancelled, dropping WaitingUser response`。**修复**：抽出唯一派发实现 `Agent.dispatchWaitingUser(ctx, busMsg)`（只用会话级 ctx；仅真正 shutdown 或 bus 满 10s 才放弃）—— WaitingUser 是**状态变更**（agent 正在提问），不是可丢弃的瞬时消息。守护 `agent/waiting_user_dispatch_test.go`：teardown（reqCtx 已取消）后仍必须送达 / shutdown 才放弃且不阻塞 / **源码级守卫**（`dispatchWaitingUser` 必须存在、函数体内不得出现 `reqCtx`、调用点不得 `dispatchWaitingUser(reqCtx` —— 旧代码下该守卫必红，有判别力）。

- **⚠️ Runner 体系已单用户化 + 会话级绑定（2026-09-17 重构，schema v69）。** 设计文档 `docs/design/runner-ssh-provisioning.md`。**核心契约**：① **runner 是全局资源**（无 `user_id`，单表 `runners`，`DROP runner_tokens`）；**会话→机器绑定是唯一权威**，存 `tenants.runner_id`（适配器 `serverapp/runner_binding.go` 实现 `tools.SessionBindingStore`，在 `server_core.go` / `server.go` 的 `SetRunnerTokenDB` 之后 `wireRunnerBindingStore(db)` 接线；内存 `SandboxRouter.sessionRunners` 只是写穿缓存，miss 时从库回填）。② **路由只按 session key**：`SandboxRouter.SandboxForSession("channel:chatID")` → 未绑定=本机、已绑定且在线=remote、**已绑定但离线=`OfflineRunnerSandbox` 硬失败**（绝不静默回退本机——历史上这一条把用户的远端任务悄悄跑在 server 上）。③ **router 的委托方法（Exec/ReadFile/…）一律返回 `errSandboxNeedsSession`**：调用方必须先用 `SandboxForSession` 解析（引擎两处：`agent/engine.go` / `agent/engine_wire.go`），禁止让 router 猜会话。④ 用户级 `active_runner` 已删除；`config action=runner sub=switch/unbind` 与 RPC `runner_session_get/set {channel,chat_id,name}`（name 为空=切回本机）是唯一切换入口。⑤ **协议版本闸门**：`runnerproto.ProtocolVersion`（当前 1），runner 连接上报 `protocol_version/name/version`，不一致直接拒绝（`RegisterRequest.UserID` 保留仅为兼容，已忽略）；端点是 `/ws`（兼容 `/ws/{x}`）。⑥ 在线状态/版本由 `tools.PopulateRunnerOnlineStatus` 从连接注册表投影，**列表绝不含 token**（`RunnerInfo` 里根本没有该字段，取 token 只能走 `RunnerStore.Token/RotateToken`）。⑦ 新增的 **`xbot-runner` 独立产物**由 `release.yml` 的 `build-runner` job 交叉编译 5 平台（此前只有 xbot-cli，导致"自动装机"物理上不可能）。
- **⚠️ 内置插件 `xbot.ssh-runner`（SSH 自动纳管远程机器）—— 控制面插件，零主模块耦合；连接模型 = VS Code Remote 式 SSH 管道（2026-09-17 用户要求）。** 源码 `plugins/xbot-ssh-runner/`（独立 Go module，**只依赖 `plugin/protocol`**）+ 前端视图 `web/src/plugins/ssh-runner/index.tsx`（esbuild → `plugin-web/xbot.ssh-runner/web/index.js`，见 `release.yml` 前端 job 与 `Makefile` 的 `plugins-web`/`plugins-install{,-builtin}`）。**runner 不是远端常驻服务**（systemd/nohup 路径已删除）：`provision` 只**安装二进制**；`connect` 起一条受管 SSH 会话，远端跑 `exec <install_dir>/xbot-runner --server … --token … --name …` ⇒ **runner 在管道前台，管道断 = runner 死**；远端脚本**开头先 `pkill -f 'xbot-runner.*--name[= ]<name>([[:space:]]|$)'`**（名字锚定 + `regexp.QuoteMeta` 转义，不会误杀 `m10`）再起新进程 ⇒ 同名永不并存；supervisor 在会话结束时**退避重连（1s→15s 封顶）**，每次重连都再杀一次老；`disconnect` 拆管道 + 杀远端进程。**`tunnel`（默认）用 `ssh -R 127.0.0.1:<rport>:127.0.0.1:<sport>` 把远端回连转发到我们这里 ⇒ 远端无需任何到 server 的网络可达性**（内网/NAT 可用；`<rport>` 在远端探测空闲端口，按 name 哈希分窗）；`direct` 才是 runner 直连 server（显式选择，**无静默回退**）。`status` 的 `service_state` 现在表达**连接状态**（`connected`/`reconnecting`/`disconnected`）+ `restarts`/`remote_port`/`last_error`；`logs` 优先返回**管道输出**（supervisor 环形缓冲，`source=ssh-session`），否则回落远端日志文件。**编排在前端**（登录身份调核心 RPC：`runner_create` 拿 `command` → `provision`（安装）→ `connect`（传 `connect_cmd=command`）→ 轮询 `status` → `runner_session_set`），因为**非 channel 的 stdio 插件不能主动调 server RPC**；channel 形态的 `context.Background()` 身份为空（会把 runner 建到空用户下），故不用。**硬约束**：插件 RPC 有 `pluginCallTimeout = 30s` 且**超时会杀掉插件进程（⇒ 所有管道一起断）**，所以 `provision`/`deprovision` 必须异步（`job_id` 轮询）、`connect` 只把 supervisor 装好即返回（长时会话在后台持有）；插件进程重启后由前端按 `autoConnect` 重新 `connect` 恢复（重连幂等）。Tests: `plugins/xbot-ssh-runner/{main_test.go,supervisor_test.go}`（隧道改写/直连不改写/杀老在新进程之前/同名 kill 模式锚定与转义/独占 Connect/Stop 远端 pkill/断线重连计数/Tail 有界/非法模式拒绝）。
- **⛔ runner 注册表是「执行目标」与「远程机器面板」的单一权威 —— 幽灵机器（`runners` 表只增不减）根治（2026-09-20 用户实测：面板 1 台真机、选择器 6 条）。** 「切换执行目标」曾读核心 `runners` 全表（`runner_list` → `tools/runner_store.go ListAllRunners`），而面板读插件自己的 `targets` ⇒ 历史/中止的纳管行**既没有清理入口（面板不显示 ⇒ UI 删不掉）又可以被选中** ⇒ 会话被绑到不存在的机器 ⇒ `SandboxRouter.SandboxForSession` 对"已绑定但离线"**硬失败**（`tools/sandbox_router.go`）⇒ **该会话每次工具调用全废**。修法（⛔ 禁止只加过滤）：① **单一权威 + 分类**：插件把受管集合（面板 `targets` 的名字）声明给核心，核心分 `managed` / `live`（在线）/ `orphan`（既不受管也不在线）——`tools/runner_registry.go:BuildRunnerRegistry` + RPC `runner_registry`（**只读，绝不自动删用户数据**：清理是用户的显式动作）；② **不可选**：`selectable = state != orphan` ⇒ 幽灵行不进选择器；受管但**离线**仍可选（绑定离线是刻意设计），但 `bar.tsx` 的 `RunnerBarPicker` **必须二次确认**并说明"绑定后每次工具调用都会失败"（`ssh-runner-bar-offline-confirm`）——绝不能让用户事后每次工具调用撞硬失败；③ **可清理**：面板「未纳管注册记录」区列出**任意**注册表行（含插件不认得的遗留行、未纳管的在线机器）+ 单条删除 + 一键清理（`runner_delete` 幂等）；④ **写入侧回滚**：`runner_create` 必须先于连接铸 token（登记行天然先于机器存在），所以探针失败 / 关掉未完成的向导必须 `discardEnrollment`（`index.tsx`；守卫：已在 `targets` 里不删、`status.connected=true` 不删、状态查询失败也不删）。⚠️ `runners.mode=docker` **不是**遗留判据（runner 侧 docker 执行器 `internal/runnerclient/docker.go` + `cmd/runner --mode docker` 仍在用），判据只有"既不受管也不在线"。Tests: `tools/runner_registry_test.go`（分类/不可选/去重/回滚零残留 + 删除幂等/列表只读）+ `web/src/plugins/ssh-runner/{bar,index}.test.tsx`（列表来源=受管集合声明、空态、离线二次确认、当前绑定是遗留时的标注、回滚、未纳管区清理）。
- **⛔ runner「所有命令 exit code 2」根治（2026-09-22 用户 P0：ssh-runner 连 GPU 机 1101 后 agent 全部命令失败）。** 根因链（生产实证，机器 8.222.11.182:1101）：① `RemoteSandbox.GetShell(_, _)` 用**空 session key** 调 `getRunnerForSession("")` → **2+ runner 在线**（b300-4 + 1101）时解析失败 → **静默 fallback 硬编码 `"/bin/sh"`**；② server 发 `["/bin/sh", "-l", "-c", cmd]` → 1101 的 `/bin/sh` 是 **dash**，login shell 读 `/etc/profile` → `/etc/profile.d/dsw_runtime_env.sh` 用 bash-only 的 process substitution `<(...)` → **dash 语法错误 exit 2，命令根本没执行**（banner 照常打印——banner.sh 排在 dsw_runtime_env.sh 之前）。**命令路由本身是对的**（ExecBg 用 `spec.SessionKey` 正确路由到 1101，banner 从 1101 回来）——只有 GetShell 用错 key。**修复四件套（"让 agent 无论用什么 runner 都和本地完全一样"）**：① `GetShell(sessionKey, _)` 用 session key 解析 runner（与 ExecBg 同一 key），解析失败**返回错误**（绝不静默猜 shell——猜错机器的 shell 比报错更糟）；② `remoteTaskOutcome`（`tools/shell.go`）：runner 报 "failed" 且 `exitCode >= 0`（命令真的跑了）→ 返回 `(exitCode, nil)`——与本地 `noneSandboxExecAsync` 对齐（非零退出是**正常完成**，由 `executeForeground` 统一格式化 `[EXIT N]`，不是 Go error）；③ runner 侧 `bgTaskManager.runNative` 的 `cmd.Dir` 复用 `resolveBgWorkDir` 降级（对齐 sync 路径 `NativeExecutor.resolveWorkDir`——目录不存在不再让命令失败）；④ runner 侧 `DetectShell` **$SHELL 优先**（对齐本地 `defaultShell()`——runner 进程从 SSH 会话继承 $SHELL，是用户真实 login shell）。**同类 bug 一并修**：`executeBangCommand`（`agent/bang_command.go`）与 PTY `selectBackend`（`channel/web/web_pty.go`）原先用 **senderID** 路由（永远匹配不到 session→runner 绑定）→ 改传 session key。守护：`tools/remote_sandbox_shell_test.go`（GetShell 按 session 解析 / 多 runner 未绑定必须报错 / remoteTaskOutcome 语义）+ `internal/runnerclient/bg_task_workdir_test.go`（目录降级 + DetectShell $SHELL 优先）。**排查口诀**：`/bin/sh: N: <file>: Syntax error` 前缀 = dash 在跑（不是 bash）；`remote task failed with exit code 2` + login banner = profile 在 dash 下炸了，先查 GetShell 解析出的 shell 是不是 /bin/sh。
- **⚠️ 本地 docker sandbox 已整体删除；sandbox 默认 `none`，隔离只走 runner（remote）（2026-09-16 用户要求，P0 根因）。** P0 现象：新建会话返回 `{"code":"internal_error","message":"CWD sync not supported in docker sandbox mode"}` —— 根因是 `agent.New` 把**空的** `cfg.SandboxMode`（`serverapp/server_core.go` 取 `cfg.Sandbox.Mode`，未配置 sandbox 的 config.json 就是空串）**写死成 `"docker"`**，而 `setCWD` 当时对任何非 `none` 模式直接拒绝。修复：① `agent/sandbox_mode.go` 的 `resolveSandboxMode` 是 Agent 侧唯一模式解析入口 —— 空值 ⇒ `none`，历史 `"docker"` ⇒ `none`（+warn 日志，配置残留不阻断启动），`remote`/`none` 原样；② 删掉 `setCWD` 的沙箱 guard（CWD 是**会话级**状态 `tenants.cwd`，与沙箱无关，本机/runner 都支持）；③ **彻底删除本地 docker sandbox 全部逻辑**：`tools/docker_sandbox.go`（986 行）+ `SandboxRouter` 的 docker 字段/`BuiltinDockerRunnerName`/`HasDocker`/`DockerImage`/docker 路由分支 + `sandbox_exec`/`shell`/`mcp_common` 的 `case "docker"` + `config.SandboxConfig` 的 `docker_image`/`host_work_dir` + `Sandbox` 接口的 export/import（`IsExporting`/`ExportAndImport`）+ `agent/registry.go` 的 `isDockerSandbox` 同步分支 + feishu/web 的内置 docker runner 展示与保护 + 相关测试。**保留**：`internal/runnerclient/*` 与 `tools/sandbox_runner.go` 的 docker 执行器 = **runner 侧** docker 模式（`mode=native|docker` 是 runner 的配置，不是本地 sandbox），`runner_tokens` 的 `mode/docker_image` 同理。**守卫**：`agent/sandbox_mode_test.go`（`resolveSandboxMode("")=="none"`、`"docker"→"none"`、`setCWD` 不再因沙箱模式拒绝 —— 修复前红灯）+ `tools/sandbox_router_test.go`（`NewSandboxRouter(config.SandboxConfig{})` 默认 `none`，绝不隐式建 docker）。
- **⚠️ `/goal` 只能在后端 pop 并执行该消息后设置 —— 发送路径禁止乐观写 goal（2026-09-16 用户报告：「发 goal 消息时（点 goal 按钮），即使消息排队也会立刻设置 goal；排队的 goal 应该 pop 之后才设置。这个按钮就是消息自动加个 `/goal` 罢了，不该干别的事情」）。** `MessageInput` 的 goal 按钮只做一件事：把消息内容加上 `/goal ` 前缀（`const content = goalMode ? `/goal ${text}` : text`）；**`AgentPanel.sendMessage` 绝不能再对该前缀做「乐观设置」（`goalEditRef.commit({objective,status:'active'})`）** —— 会话 busy 时消息会进排队队列（`queued=true`，尚未分配 turn_id），此时 goal 并未生效；乐观写入会让 banner 在排队期间就显示新目标。goal 的**唯一**生效路径是后端 `processMessage` 执行 `/goal` 命令后 push 进度（`progress.goal`）或 `get_goal` 回读。**显式编辑**（GoalBanner 编辑框 / todo 的「设为目标」→ `setGoal` RPC）保持乐观覆盖不变（那是直接 RPC，立即生效）—— `usePendingEdit` 的语义只服务这条路径。守卫：`web/e2e/goal-todo-optimistic.spec.ts` 的「REPRO: 排队中的 goal 消息不得立刻设置 goal（pop/后端 push 后才生效）」（busy 会话 + goal 模式发送 ⇒ 断言 **goal banner 元素**（`[data-testid="goal-text"]`）仍为旧目标；后端 push 后才变新目标；mutation：把乐观块加回 ⇒ 该用例红）。⚠️ 断言必须锁定 banner 元素而非裸文本：消息正文本身就是 `/goal <目标>`，排队队列里当然会出现那段文本（我第一版断言 `getByText(newGoal)` 因而假红）。
