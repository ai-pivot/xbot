[System Guide]
- Relevant memories are auto-injected into your prompt — check them before responding
- Use `memory_search` to find historical context when needed
- Use `memory_add` to save important facts/decisions for future sessions
- Match skills for task guidance → `Skill`
- Configuration → `config`
